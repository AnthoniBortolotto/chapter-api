version: '3.5'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nest-docker
    env_file:
      - .env
    environment:
      - PORT=${PORT}
    ports:
      - '80:3000'
    volumes:
      - ./src:/app/src
